# Example production environment variables (do NOT commit secrets)
# Replace the example values with your real production URLs and secrets.

# URL pública do frontend (usada para gerar links no backend, e-mails etc.)
APP_BASE_URL=https://your-frontend.example.com

# Base URL da API usada pelo frontend (inclua /api se o backend expõe em /api)
# Ex: https://api.your-domain.com/api
VITE_API_BASE=https://your-backend.example.com/api

# URL pública do backend (opcional, usada por ferramentas de build / proxy)
BACKEND_URL=https://your-backend.example.com

# Em desenvolvimento local você pode definir `FRONTEND_DEV_URL` se necessário.
# FRONTEND_DEV_URL can be set for local dev tooling (optional).

# CORS: origins permitidos (comma-separated)
ALLOWED_ORIGINS=https://your-frontend.example.com

# Segredo JWT (mantenha seguro e diferente do valor de exemplo)
JWT_SECRET=replace_with_a_secure_random_value

# NODE_ENV padrão para produção
NODE_ENV=production

# Observação: não adicione este arquivo ao repositório com valores reais.
# Use a interface do provedor (Heroku/Render/Netlify/Vercel) ou arquivos
# .env privados no servidor para armazenar valores sensíveis.
